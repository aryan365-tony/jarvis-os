"""Jarvis-OS shell package.

The shell is an AI-first, voice-first kiosk interface. Its guiding principle is
*progressive availability*: the UI must appear and accept input immediately,
then gain capabilities (model, voice, memory, widgets) as background services
come online. Nothing in the interface may block on a backend being ready.
"""

__version__ = "0.2.0"
